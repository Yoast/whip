name: Test

on:
  # Run on relevant pushes to `main` and on all relevant pull requests.
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
      - '.phpcs.xml.dist'
      - 'phpcs.xml.dist'
      - 'config/**'
      - '.github/dependabot.yml'
      - '.github/workflows/cs.yml'
      - 'images/**'
  pull_request:
    paths-ignore:
      - '**.md'
      - '.gitattributes'
      - '.gitignore'
      - 'LICENSE'
      - '.phpcs.xml.dist'
      - 'phpcs.xml.dist'
      - 'config/**'
      - '.github/dependabot.yml'
      - '.github/workflows/cs.yml'
      - 'images/**'
  # Allow manually triggering the workflow.
  workflow_dispatch:

# Cancels all previous workflow runs for the same branch that have not yet completed.
concurrency:
  # The concurrency group contains the workflow name and the branch name.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        php_version: ['5.4', '5.5', '5.6', '7.0', '7.1', '7.3', '7.4', '8.0', '8.1', '8.2', '8.3', '8.4']
        coverage: [false]

        # Run code coverage only on high/medium/low PHP.
        include:
        - php_version: 5.3
          coverage: true
        - php_version: 7.2
          coverage: true
        - php_version: 8.5
          coverage: true

    name: "Lint and test: PHP ${{ matrix.php_version }}"

    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install PHP
        uses: shivammathur/setup-php@44454db4f0199b8b9685a5d763dc37cbf79108e1 # 2.36.0
        with:
          php-version: ${{ matrix.php_version }}
          ini-values: zend.assertions=1, assert.exception=1, error_reporting=-1, display_errors=On, display_startup_errors=On
          coverage: ${{ matrix.coverage == true && 'xdebug' || 'none' }}
          tools: cs2pr
        env:
          fail-fast: true

      # YoastCS 3.0 has a PHP 7.2 minimum which conflicts with the requirements of this package.
      - name: 'Composer: remove YoastCS'
        run: composer remove --dev yoast/yoastcs --no-update --no-interaction

      # Related to PHPUnit CVE https://github.com/advisories/GHSA-vvj3-c3rp-c85p
      # No "safe" versions were released for PHPUnit < 8.x, which means that the Roave security advisories
      # package will now block installation of the PHPUnit versions we need to test on PHP 7.1 and lower.
      # While correct, in this case, running the tests takes precedence over the negligible security
      # risk, as the security issue is related to `phpt` tests, which are not used in this package.
      - name: 'Composer: remove security advisories'
        if: ${{ matrix.php_version <= '7.1' }}
        run: composer remove --dev roave/security-advisories --no-update --no-interaction

      # Install dependencies and handle caching in one go.
      # @link https://github.com/marketplace/actions/install-php-dependencies-with-composer
      - name: Install Composer dependencies
        uses: ramsey/composer-install@3cf229dc2919194e9e36783941438d17239e8520 # 3.1.1
        with:
          # Bust the cache at least once a month - output format: YYYY-MM.
          custom-cache-suffix: $(date -u "+%Y-%m")

      - name: Lint against parse errors
        run: composer lint -- --checkstyle | cs2pr

      - name: Run the unit tests
        if: ${{ matrix.coverage == false }}
        run: composer test

      - name: Run the unit tests with code coverage
        if: ${{ matrix.coverage == true }}
        run: composer coverage

      - name: Upload coverage results to Coveralls
        if: ${{ success() && matrix.coverage == true }}
        uses: coverallsapp/github-action@5cbfd81b66ca5d10c19b062c04de0199c215fb6e # v2.3.7
        with:
          format: clover
          file: build/logs/clover.xml
          flag-name: php-${{ matrix.php_version }}
          parallel: true

  coveralls-finish:
    needs: test
    runs-on: ubuntu-latest

    name: Coveralls Finish

    steps:
      - name: Coveralls Finished
        uses: coverallsapp/github-action@5cbfd81b66ca5d10c19b062c04de0199c215fb6e # v2.3.7
        with:
          parallel-finished: true
